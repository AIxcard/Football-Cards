services:
  - type: web
    name: football-cards
    env: node
    plan: free
    buildCommand: echo "Build successful"
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
